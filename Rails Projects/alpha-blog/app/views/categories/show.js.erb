// Append new data
$("<%= j render('articles/show-articles', obj: @category_articles) %>")
  .appendTo($("#categoryArticles"));

// Update pagination link
<% if @articles.last_page? %>
  $('.pagination').html("That's all, folks!");
<% else %>
  $('.pagination')
    .html("<%= j link_to_next_page(@category_articles, 'Next Page', remote: true) %>");
<% end %>
